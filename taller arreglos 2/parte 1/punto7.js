/* Cree una función que reciba como argumento el siguiente arreglo [2, 5, 7, 9] y lo recorra, imprimiendo cada uno de sus elementos. */
function arreglo() {
    let array=[2, 5, 7, 9];
    for (let index = 0; index < array.length; index++) {
        console.log(array[index]);        
    }    
}
arreglo();
